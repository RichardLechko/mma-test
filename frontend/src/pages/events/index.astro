---
import Layout from '../../layouts/Layout.astro';
import { supabase } from '../../lib/supabase';

// Get the current year for the redirect
const currentYear = new Date().getFullYear();

// Redirect to the current year's events page
return Astro.redirect(`/events/${currentYear}`);
---